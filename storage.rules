rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Captured photos/videos: public read (customer gallery), authenticated write (staff console).
    match /jobs/{jobId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
